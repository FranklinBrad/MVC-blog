<section>
  <div>
    <h2> Recent posts</h2>
    {{#each posts}}
    <div class="card-body">
      <a href="/posts/{{id}}">{{post_title}}</a>
      <p>{{post_body}}</p>
      <p>created by: {{User.username}} on {{format_date createdAt}}</p>
    </div>
    {{/each}}
  </div>
</section>